# Application Resources

This directory contains configuration files and runtime assets bundled with the Poorcraft Ultra application module.

## Files

- `application.conf` – Primary configuration used by the engine at launch.

## Notes

Generated assets (textures/skins) are stored outside of this module under the project-level `assets/` directory and are not checked into version control.
