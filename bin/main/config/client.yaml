# Poorcraft Ultra Client Configuration (Default Embedded)
# This file is embedded in the JAR and loaded from classpath by default.
# To override: create config/client.yaml in the application directory.

# Display settings
displayWidth: 1280      # Window width in pixels
displayHeight: 720      # Window height in pixels
fullscreen: false       # Start in fullscreen mode
vsync: true             # Enable vertical sync
fpsLimit: 60            # Maximum frames per second

# Logging level (DEBUG, INFO, WARN, ERROR)
logLevel: INFO
