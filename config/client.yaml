# Poorcraft Ultra Client Configuration
# This file is loaded by ConfigLoader; if missing, defaults are used

# Display settings
displayWidth: 1280      # Window width in pixels
displayHeight: 720      # Window height in pixels
fullscreen: false       # Start in fullscreen mode
vsync: true             # Enable vertical sync
fpsLimit: 60            # Maximum frames per second

# World loading
loadMultiChunk: true    # Load 3x3 chunk grid when true; single chunk when false

# World persistence
worlds:
  baseDir: data/worlds  # Base directory for world saves

# Logging level (DEBUG, INFO, WARN, ERROR)
logLevel: INFO

# Input controls (Phase 1.5)
controls:
  mouseSensitivity: 1.5
  invertMouseY: false
  keybinds:
    moveForward: W
    moveBackward: S
    moveLeft: A
    moveRight: D
    sprint: LSHIFT
    breakBlock: MOUSE_LEFT
    placeBlock: MOUSE_RIGHT
    pause: ESCAPE

# Graphics settings (Phase 1.5)
graphics:
  windowMode: WINDOWED  # WINDOWED, FULLSCREEN, BORDERLESS
  resolution: "1280x720"
  vsync: true
  fpsLimit: 60
